.dashboard
  .main-col
    .events
      = render @events or t('events.empty')
  .side-col
    = link_to t(".projects"), projects_path, class: 'to-projects'
    .projects
      = render partial: 'projects/project', collection: @projects, as: :project, layout: 'projects/list_item' || t('projects.empty')
